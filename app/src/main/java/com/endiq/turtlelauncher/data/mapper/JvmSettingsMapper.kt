package com.endiq.turtlelauncher.data.mapper

import com.endiq.turtlelauncher.data.jvm.JvmSettings as DataJvmSettings
import com.endiq.turtlelauncher.domain.model.JvmSettings as DomainJvmSettings

fun DataJvmSettings.toDomain(): DomainJvmSettings = DomainJvmSettings(
    maxHeapMb = maxHeapMb,
    minHeapMb = minHeapMb,
    useG1GC = useG1GC,
    gcPauseMillis = gcPauseMillis,
    parallelRefProc = parallelRefProc,
    heapRegionSizeMb = heapRegionSizeMb,
    disableClouds = disableClouds,
    extraJvmArgs = extraJvmArgs,
    mouseSensitivity = mouseSensitivity,
    renderDistance = renderDistance,
    graphicsMode = graphicsMode,
    cacheDirPath = cacheDirPath,
    unlockFps = unlockFps,
    maxFps = maxFps,
    enableVsync = enableVsync,
    framePacing = framePacing,
    frameSkipping = frameSkipping,
    adaptiveFrameTiming = adaptiveFrameTiming,
    lowLatencyRendering = lowLatencyRendering,
    performanceProfileLocked = performanceProfileLocked,
    mipmapLevels = mipmapLevels,
    entityDistanceScaling = entityDistanceScaling,
    simulationDistance = simulationDistance,
    particles = particles,
    ambientOcclusion = ambientOcclusion,
    biomeBlendRadius = biomeBlendRadius,
    fastMath = fastMath,
    threadedChunkBuild = threadedChunkBuild,
    shaderCache = shaderCache,
    rendererDebugLogging = rendererDebugLogging,
    fullscreen = fullscreen,
    resolutionScalePercent = resolutionScalePercent,
)

fun DomainJvmSettings.toData(): DataJvmSettings = DataJvmSettings(
    maxHeapMb = maxHeapMb,
    minHeapMb = minHeapMb,
    useG1GC = useG1GC,
    gcPauseMillis = gcPauseMillis,
    parallelRefProc = parallelRefProc,
    heapRegionSizeMb = heapRegionSizeMb,
    disableClouds = disableClouds,
    extraJvmArgs = extraJvmArgs,
    mouseSensitivity = mouseSensitivity,
    renderDistance = renderDistance,
    graphicsMode = graphicsMode,
    cacheDirPath = cacheDirPath,
    unlockFps = unlockFps,
    maxFps = maxFps,
    enableVsync = enableVsync,
    framePacing = framePacing,
    frameSkipping = frameSkipping,
    adaptiveFrameTiming = adaptiveFrameTiming,
    lowLatencyRendering = lowLatencyRendering,
    performanceProfileLocked = performanceProfileLocked,
    mipmapLevels = mipmapLevels,
    entityDistanceScaling = entityDistanceScaling,
    simulationDistance = simulationDistance,
    particles = particles,
    ambientOcclusion = ambientOcclusion,
    biomeBlendRadius = biomeBlendRadius,
    fastMath = fastMath,
    threadedChunkBuild = threadedChunkBuild,
    shaderCache = shaderCache,
    rendererDebugLogging = rendererDebugLogging,
    fullscreen = fullscreen,
    resolutionScalePercent = resolutionScalePercent,
)
